'use client';
import HeroContent from './HeroContent';

const Hero = () => {
  return (
    <div className="relative flex h-full w-full flex-col" id="about-me">
      <HeroContent />
    </div>
  );
};

export default Hero;
